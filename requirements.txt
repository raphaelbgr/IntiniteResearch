# Core Agno Framework
agno>=2.3.0

# OpenAI client for LMStudio compatibility
openai>=1.0.0

# DuckDuckGo search for parallel web search (renamed to ddgs)
ddgs>=0.1.0

# Tavily search API for AI-optimized web search (optional, parallel provider)
tavily-python>=0.3.0

# Pydantic for structured data models
pydantic>=2.0.0

# SQL Alchemy for Agno storage
sqlalchemy>=2.0.0

# Vector database support
pgvector>=0.3.0  # For PostgreSQL vector support (optional)
psycopg2-binary>=2.9.0  # PostgreSQL adapter (optional)

# SQLite vector extension (lightweight alternative)
sqlite-vec>=0.1.0

# Async support
aiofiles>=23.0.0
asyncio>=3.4.3

# Configuration and utilities
pyyaml>=6.0
python-dotenv>=1.0.0

# Text processing
pypdf>=4.0.0  # For PDF knowledge sources
beautifulsoup4>=4.12.0  # For web scraping
requests>=2.31.0

# Embeddings (if not using LMStudio for embeddings)
sentence-transformers>=2.5.0  # Local embeddings

# Progress and logging
tqdm>=4.66.0
rich>=13.0.0  # Beautiful terminal output

# Date/time utilities
python-dateutil>=2.8.0

# Optional: For advanced features
# fastapi>=0.109.0  # If you want to add API endpoints
# uvicorn>=0.27.0  # ASGI server for FastAPI
